---
name: image2prompt
description: 围绕参考图产出生图提示词的三合一 skill：①从设计精美、风格难以言说的图片反推可复用的"风格DNA"和完整提示词；②用固定修图指令一次性优化手机拍摄的问题照片（拉直构图、平衡光源、提升清晰度和饱和度）；③在不动原图的前提下换主体/删元素/整体微调。输出国产模型版（即梦/豆包/可灵）和 GPT Image 2 版。只要用户给出图片（路径、URL 或直接粘贴）并想要：反推/提取提示词、问"这是什么风格"、照这个风格再做一张、修图/调色/拉直/变清晰、换主体换场景、更有广告感、为广告提案或 PPT 配图——都应使用本 skill，即使用户没说"提示词"三个字。
---

# image2prompt：参考图反推提示词，改写后投喂生图模型

## 核心定位

用户（广告从业者）拿一张参考图来，想要的东西分三种：把说不出名字的好设计变成可复用的提示词；把拍坏的照片一次修好；或者要一张"差不多但不一样"的图。你的产出是**可直接粘贴使用的生图提示词**，不是图。质量标准：用户复制后不需要再改一个字。

两条铁律：

- **识图必须原生**：本 skill 同时安装在 Claude Code 和 Codex 上，Claude 用 `Read` 工具读图，Codex 用原生多模态查看。禁止凭记忆或想象猜测图片内容——必须真的把图看进来再分析。
- **客观的自己定，主观的问用户**：问题诊断、风格判别是客观分析，直接做；裁切范围、色调取向、保留什么强调什么是用户的审美和用途，给出候选和推荐后让用户拍板。替用户裁掉他还想看的东西，比没修更糟。

## 第 1 步：拿到图片

- 本地路径 → 直接原生读图。
- URL → 先下载：`curl -sL "<URL>" -o /tmp/ref-img.<原扩展名>`，再读。
- 直接粘贴 → 消息里已能看到图，直接分析。
- 一张图都没有 → 请用户提供，不要凭空开始。

## 第 2 步：判断本次是哪种任务

| 信号 | 模式 |
|---|---|
| "这是什么风格""照这个感觉来一张""反推提示词"，且图是精心设计的海报/KV/渲染图 | **A 风格反推** |
| "修一下""拉直""太糊了""颜色不好看"，或图本身有明显质量问题（歪、糊、暗、偏色、杂乱） | **B 照片优化** |
| "换成 X""去掉 Y""改一下 Z"，或者给了图但需求说不清 | **C 改图微调** |

组合很常见（手机拍的图既要修又要换品牌 = B+C）：各自流程都走，输出合并。判断不出来时按 C 处理并在展示配方后问用户。

## 模式 A：风格反推（设计精美、风格难以描述的图）

核心价值：**把风格和内容拆开**。用户看得出这张图好看，但说不出为什么——你的任务是把"好看"翻译成可复用的文字资产。

1. 基础配方提取（见"图像配方"一节）。
2. 读 `references/design-styles.md`，深挖设计语言：给风格命名（最接近的 1-2 个流派 + 判断依据；混合风格就说"主体流派 + 借用元素"），再按深挖清单补充：字体排印、图形语言、色板、质感渲染、构成网格。**形态描述过一遍避坑清单**——几何圆角和有机液态是两种气质，写错一个词生成方向就全偏。
3. 产出**风格 DNA**：一段与画面内容无关、可套用任何主体的风格描述块。检验标准：把任何新主体塞进去都成立。这是模式 A 的核心交付。
4. 完整提示词：用户给了新主体就套新主体；没给就用原图内容示范。**同时给一条垫图风格参考指令**（上传原图 + 点名要保持的形态特征）——形态这类"只可意会"的特征，让模型看一眼原图比纯文字描述准得多，文生图跑偏时这是第一修复路线。

## 模式 B：照片优化（手机拍摄的问题图）

这个模式不反推、不重构，目标是**垫着原图一次修好**。

1. **诊断**（客观层）：对着图把问题列具体——哪里歪、哪块欠曝过曝、偏什么色、糊在哪、什么杂物。写成清单，不要笼统说"质量不佳"。
2. **先问再开方**（主观层）：三类决定交给用户拍板，用户需求里已说清楚的不重复问：
   - **景别与裁切**：保持原构图完整视野只做矫正（默认），还是收紧构图突出主体？
   - **色调取向**：白平衡校正到中性标准光，还是保留现场光的氛围？
   - **保留与强调**：哪些信息必须完整保留，哪些要清理，哪些要重点强调？
   每类给候选项和推荐理由，等用户确认再写指令。
3. 读 `references/photo-fix.md`：从固定基础指令起步，删掉不适用条目，把诊断结果和用户的决定改写成针对性描述（白平衡顽固偏色时用白色锚点写法）。
4. **只输出一条垫图编辑指令**（GPT Image 2 上传原图；即梦/豆包图生图同样适用）。此模式不出文生图版——重画会丢掉照片里的真实内容。

## 模式 C：改图微调（换主体 / 删元素 / 需求不明确）

1. 基础配方提取（见"图像配方"一节）。
2. 确定改法：
   - 用户已说明改动 → 直接改配方对应维度，其余原样保留。
   - 模糊需求 → 结合已判别的图片类型，按下表落成具体改法：

   | 用户说 | 实际改法 |
   |---|---|
   | 更有体感 / 体验感 | 加入使用场景和人的交互（手部动作最稳）、环境自然光、真实质感细节（水珠、蒸汽、织物纹理） |
   | 更有广告感 | 影棚级光线、干净背景、主体突出、构图留文案位、色调统一 |
   | 更高级 | 低饱和色调、大面积留白、柔和侧光、极简背景 |
   | 更有冲击力 | 低角度仰拍、强明暗对比、饱和撞色、动态元素、加大主体占比 |
   | 更温暖 / 有人味 | 暖色调、自然光、人物与微表情、浅景深、生活化场景 |

   - 需求不明确或什么都没说 → 展示图像配方，结合该图类型的"生成要点"**主动给 2-3 个具体改进方向**让用户挑，不要干等。
3. 双版本输出。**垫图编辑指令必须按 `references/gpt-image.md` 的"边界控制"写**：第一句圈范围（视觉锚点）→ 锁保护清单（点名不许动的元素及其原有内容）→ 枚举替换目标（禁用"所有/全部"）→ 禁增禁删条款。编辑模型有"主题一致化"倾向，不锁边界它会把无关元素一起和谐掉。

## 图像配方（模式 A / C 共用的提取底稿）

先读 `references/ad-types.md`，然后完成两件事：

**判别广告图类型**：产品 hero shot / 场景使用图 / 情绪海报 / 背景图 / 促销 banner / 概念创意图。不确定时给最接近的一类并说明存疑点。

**按 9 个基础维度提取，再叠加该类型的强化维度**：

1. 主体（是什么、外观特征、位置和占比）
2. 场景背景（环境、背景元素、虚实）
3. 构图景别（视角、特写/中景/全景、估计画幅比例）
4. 光线（光源方向、软硬、明暗对比）
5. 色调（主色辅色、饱和度、冷暖）
6. 风格媒介（摄影/插画/3D/平面设计及具体风格）
7. 材质细节（最突出的质感表面）
8. 画面文字（文字、logo、排版元素及位置，没有就写无）
9. 氛围情绪（整体传达的感受）

类型库里每个类型都有"强化提取"清单（如情绪海报抓留白位置和文案位、背景图抓可叠加安全区），判别出类型后把对应维度补进配方。内置风格只在识别阶段强化，不当生成模板套。

某个维度对本次需求关键但一眼看不准的，回到图上重点观察再补充，不要含糊带过。

## 输出格式（按模式组装）

共同规则：所有提示词一律放代码块方便一键复制；末尾"使用提示"一两行说明推荐版本、操作方式（垫图/文生图）、建议画幅比例。生成前先读对应规范：`references/jimeng.md`（国产模型版）和 `references/gpt-image.md`（GPT Image 2 版）。

- **模式 A**：图像配方（含风格命名与深挖）→ 风格 DNA（代码块）→ 国产模型版 → GPT Image 2 版（文生图 + 垫图风格参考指令）→ 使用提示
- **模式 B**：问题诊断清单 → 三类主观决定的提问（用户确认后）→ 修图指令（一条，代码块）→ 使用提示
- **模式 C**：图像配方（用【改】标出动过的维度）→ 国产模型版（主提示词 + 1 条变体）→ GPT Image 2 版 → 使用提示
- **B+C 组合**：诊断和改动合并成一条编辑指令；改动幅度大时建议分两轮（先修图后改图），把两轮指令都给出。

模式选择逻辑（A/C 的 GPT Image 2 版）：换主体、改局部 → 垫图编辑指令，最能保住原图构图光影质感；整体重构风格、原图只是灵感 → 文生图写法。

## 结果不对时（迭代路径）

用户拿生成结果回来反馈时，先对照原图把偏差点名到维度（形态？密度？边界溢出？偏色？），再选修复路线：

1. **改词重跑**：定位是哪个词把模型带偏的（实测案例："液态膨胀"→ 有机泡泡字），换成更精确的描述。
2. **升级到垫图**：文生图两轮都不像 → 上传原图做风格参考/编辑基底，点名要保持的特征。
3. **工具级边界**：编辑溢出屡禁不止 → 平台涂选区域（局部重绘）、分轮编辑、或后期拼合（原图做底，只抠回改对的区域）。

把本次学到的通用教训写回对应 reference 文件——skill 的修正是复利的。

## 注意事项

- 广告可用性优先于复刻精度：主动考虑留文案位、主体清晰、画面干净，这比 100% 还原参考图更重要。
- 参考图常是竞品或版权素材：反推的目的是提取风格语言。提示词中不要出现原图的品牌名和 logo 文字（除非用户明确要求），涉及替换时明确要求"去除原有品牌痕迹"。
- 读不到图（本地路径不存在、URL 下载失败）→ 让用户确认路径或重新提供，绝不凭想象编造图片内容。
